TODO 
- Add tolbar state from planner or from lexical playground
    Call a webview native method to post the lexical editor state and call the save to db on the native method
- Add more toolbar items from playground
- Add a long press button for option like H1, H2, H3, etc
- Add a color picker for highlight
- Add a new node to transform first line into a title https://lexical.dev/docs/api/modules/lexical_react_LexicalAutoLinkPlugin
- Get lexical state into the component and call the db functions to save it
    - Use state to store the ID for current note and decide to create new or only update

- Look chat gpt topics